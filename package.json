{
  "name": "@triargos/linear-integrations",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "prettier --write \"packages/**/*.ts\" \"shared/**/*.ts\"",
    "linear": "pnpm --filter @triargos/effect-linear",
    "outlook": "pnpm --filter @triargos/linear-outlook-sync",
    "ingest": "pnpm --filter @triargos/linear-issue-ingest",
    "connect-import": "pnpm --filter @triargos/linear-connect-customer-import"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "dependencies": {
    "prettier": "^3.6.2"
  }
}